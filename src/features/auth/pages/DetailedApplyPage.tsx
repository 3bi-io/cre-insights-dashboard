import React from 'react';
import { DetailedApplicationForm } from '@/components/apply/detailed';

/**
 * DetailedApplyPage - Route handler for /apply/detailed
 * Delegates to DetailedApplicationForm for the actual implementation
 */
const DetailedApplyPage = () => {
  return <DetailedApplicationForm />;
};

export default DetailedApplyPage;
