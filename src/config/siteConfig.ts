/**
 * Site Configuration
 * Single source of truth for domain, branding, and default SEO values
 */

export const SITE_URL = 'https://applyai.jobs';
export const SITE_NAME = 'Apply AI';
export const DEFAULT_OG_IMAGE = `${SITE_URL}/og-image.png`;
export const DEFAULT_LOGO = `${SITE_URL}/logo.png`;
